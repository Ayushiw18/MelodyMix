# MelodyMix
PlaylistManager is a console-based Java application that allows users to manage a playlist, providing features such as adding, deleting, searching, sorting songs, and keeping track of recently played tracks. This application utilizes a doubly linked list to store the playlist and provides persistent storage through a file (playlist.txt) to savedata
